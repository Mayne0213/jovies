apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jovies-dev

resources:
  - ../../base
  - resourcequota.yaml
  - namespace.yaml
  - ingress.yaml

commonLabels:
  environment: development

# 이미지 태그 설정
images:
  - name: github.com/Mayne0213/jovies
    newTag: develop-sha-71e158e9ea7911c8e9b90c9a8c41d9c31b2bfa14

patchesStrategicMerge:
  - deployment-patch.yaml
