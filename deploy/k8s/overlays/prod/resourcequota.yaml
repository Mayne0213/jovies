apiVersion: v1
kind: ResourceQuota
metadata:
  name: jovies-quota
  namespace: jovies
spec:
  hard:
    requests.memory: "200Mi"
    requests.cpu: "200m"
    limits.memory: "300Mi"
    limits.cpu: "400m"
    pods: "3"
